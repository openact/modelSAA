# list of global parameters, switches, etc for a run
# flags - map[string]string
globals:
  variableSets:
     calculatedVariables:
        - ALM
     storedVariables:
        - ALM_KRI_1
        - ALM_KRI_2
     aggregatedVariables:
        - ALM_KRI_1
        - ALM_KRI_2
